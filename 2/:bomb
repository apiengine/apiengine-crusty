#!/bin/sh

bomb_usage() {
    __1=$1; shift

    echo "$__1 : usage : ${AE_CMD} {c,r,u,d} $*"
    exit 1
}

bomb_fail() {
    echo error: $1
    exit 2
}

bomb_retok() {
    if [ $1 != 0 ]; then
        fail "$2 command failed."
    fi
}
