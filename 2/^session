#!/bin/sh

session_csrf() {
    if [ -f ${AE_SESSION} ]; then
        local json=`< ${AE_SESSION} cat`
        json_v ${json} "csrf"; local csrf=$__
        AE_CSRF="-H \"X-CSRF-Token:${csrf}\""
    fi
}

session_csrf
